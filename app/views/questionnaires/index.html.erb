<p id="notice"><%= notice %></p>

<h1>Questionnaires</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Projectrepo</th>
      <th>Projecturl</th>
      <th>Usingbestpracticesforopp</th>
      <th>Modulardevelopment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @questionnaires.each do |questionnaire| %>
      <tr>
        <td><%= questionnaire.name %></td>
        <td><%= questionnaire.email %></td>
        <td><%= questionnaire.projectrepo %></td>
        <td><%= questionnaire.projecturl %></td>
        <td><%= questionnaire.usingbestpracticesforopp %></td>
        <td><%= questionnaire.modulardevelopment %></td>
        <td><%= link_to 'Show', questionnaire %></td>
        <td><%= link_to 'Edit', edit_questionnaire_path(questionnaire) %></td>
        <td><%= link_to 'Destroy', questionnaire, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Questionnaire', new_questionnaire_path %>
